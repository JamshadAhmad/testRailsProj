<div class="comment-box">
  <strong><%= comment.name %></strong> : <%= comment.comment %>

  <br>

  <%= link_to 'Delete', [comment.post, comment], method: :delete, class: "btn-danger", data: { confirm: "Are you sure?"} %>
</div>